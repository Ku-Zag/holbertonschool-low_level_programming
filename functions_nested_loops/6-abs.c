#include "main.h"

/**
 * _abs - Entry point
 *
 * @a: integer
 * Description: i do knwo
 * Return: 0
 */

int _abs(int a)
{
	if (a >= 0)
	{
		a = a;
	}
	else if (a < 0)
	{
		a = -a;
	}
	return (a);
}
