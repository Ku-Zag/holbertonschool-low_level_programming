#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 */
char *argstostr(int ac, char **av)
{
	char *ptr;
	int na = 0;
	int l = 0;

	if (ac == 0 || av == NULL)
		return (NULL);
	for (; na < ac; na++)
	{
		ZZ
		av[l]
	}
	ptr = (char**)malloc(sizeof(char *) * l);
	return (ptr);
}
