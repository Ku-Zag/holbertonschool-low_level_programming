#include "main.h"
#include <stdlib.h>
/**
 */
int *array_range(int min, int max)
{
	int *all;
	int l = 0;

	if (min > max)
		return (NULL);
	all = malloc(sizeof(int) * l);

	if (all == NULL)
		return (NULL);
	
}
